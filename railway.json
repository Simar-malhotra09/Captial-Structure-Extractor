{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "startCommand": "pip install -r requirements.txt && uvicorn server:app --host 0.0.0.0 --port ${PORT:-8000}",
    "healthcheckPath": "/api/health",
    "restartPolicyType": "ON_FAILURE"
  }
}
